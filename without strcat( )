

#include <stdio.h>
#include<string.h>
int main() {
    char s[10],r[20],a[30];
    int i,j;
    scanf("%s",s);
    scanf("%s",r);
    for(i=0;s[i]!='\0';i++){
        a[i]=s[i];
    }
    for(j=0;r[j]!='\0';j++){
        a[i+j]=r[j];
    }
    printf("%s",a);
}
